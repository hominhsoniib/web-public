VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_SITE_URL=https://sambaden.vn

# Offline-fallback cho /portal/login khi KHÔNG có backend phản hồi (network
# error/timeout) — xem src/lib/portalApi.ts. Copy sang .env.local (không commit)
# và điền giá trị thật. TẠM THỜI — migrate sang Vercel Serverless Function khi
# có backend thật (xem TODO trong portalApi.ts).
VITE_OFFLINE_ADMIN_EMAIL=admin@example.com
VITE_OFFLINE_ADMIN_PASSWORD_SHA256=<sha256-hex-cua-mat-khau>

